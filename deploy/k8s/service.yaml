apiVersion: v1
kind: Service
metadata:
  name: cancer-mama-api
  labels:
    app: cancer-mama-api
spec:
  selector:
    app: cancer-mama-api
  ports:
    - name: http
      port: 80
      targetPort: http
  type: ClusterIP
