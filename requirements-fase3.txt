# Dependencias pesadas exclusivas do fine-tuning da Fase 3 (assistente medico).
# Mantidas fora de requirements.txt de proposito para nao inflar/quebrar o
# job de CI existente (Fases 1/2), que nao precisa delas. Instale so quando
# for rodar `fase3/finetuning/train_lora.py` ou usar o backend "local" de
# `fase3/llm_backend.py`:
#
#   pip install -r requirements.txt -r requirements-fase3.txt
#
# O adapter promovido foi treinado em CUDA/FP16 numa RTX 3060 de 12 GB.
# Para reproduzir em GPU, instale previamente uma build CUDA compativel do
# PyTorch; a mesma lista tambem permite inferencia e testes em CPU.
torch>=2.6,<3.0
transformers>=4.44,<5.0
peft>=0.12,<1.0
datasets>=2.20,<3.0
accelerate>=1.0,<2.0
